# Tooling dependencies (glossary extraction and similar text analysis)
#
# Requires: MeCab shared library (libmecab) on the host OS.
#   AlmaLinux/RHEL 9: sudo dnf install mecab   (mecab-devel not in AppStream)
#   Debian/Ubuntu: sudo apt install mecab libmecab-dev
#   macOS: brew install mecab
# Dictionary: unidic-lite (pip) — mandatory for this project.
#   Optional full UniDic: pip install unidic && python -m unidic download

fugashi>=1.3.0
unidic-lite>=1.0.8

# glossary-config.json validation (scripts/glossary_extractor.py)
# Install via: python -m pip install -r requirements-dev.txt
# Do not rely on ad-hoc "python3 -m pip install jsonschema" on system Python alone —
# use the project .venv so CLI and MCP share the same interpreter.
jsonschema>=4.23.0

# Legacy fallback — NOT used when morphology.required=true (default).
# janome>=0.5.0
