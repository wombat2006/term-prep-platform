{
  "name": "@term-prep/googledrive-connector",
  "version": "0.1.0",
  "private": true,
  "description": "Google Drive corpus mirror for term-prep-platform (Phase 0.5)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "term-prep-drive": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test test/no-credentials.test.mjs",
    "drive:mirror": "node dist/cli.js mirror",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "google-auth-library": "^9.15.1",
    "googleapis": "^126.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.4",
    "typescript": "~5.3.3"
  }
}
